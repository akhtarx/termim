name: Release

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build - ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            suffix: ""
            name_os: linux
            name_arch: x86_64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            suffix: ".exe"
            name_os: windows
            name_arch: x86_64
          - os: macos-latest
            target: x86_64-apple-darwin
            suffix: ""
            name_os: macos
            name_arch: x86_64
          - os: macos-latest
            target: aarch64-apple-darwin
            suffix: ""
            name_os: macos
            name_arch: aarch64

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      
      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Prepare Artifact
        shell: bash
        run: |
          BINARY_NAME="termim-${{ matrix.name_os }}-${{ matrix.name_arch }}${{ matrix.suffix }}"
          # Use a more flexible copy to find the binary regardless of host/target nesting
          find target -name "termim${{ matrix.suffix }}" -path "*/release/*" -exec cp {} $BINARY_NAME \;
          echo "BINARY_PATH=$BINARY_NAME" >> $GITHUB_ENV
          echo "Created artifact: $BINARY_NAME"

      - name: Upload Release Asset
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/v')
        with:
          files: ${{ env.BINARY_PATH }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
